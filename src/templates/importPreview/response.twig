<h2>Entries</h2>
{% for index, importPreviewEntry in importPreview.entries %}
    {% include 'import/importPreview/entry' with {
        index: index,
        importPreviewEntry: importPreviewEntry,
    } only %}
{% endfor %}

<hr/>
<input type="hidden" id="ImportPreview-sectionMapping" value="{{ importPreview.sectionMapping | json_encode }}" />
<input type="hidden" id="ImportPreview-entries" value="{{ importPreview.entries | json_encode }}" />
{% include 'import/importPreview/submitButton' %}